name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      # A handful of root-level tests (adminFrontendHelpers, wizmatchRouteRegistry,
      # wizmatchFilterPipeline, wizmatchPlacementCommercials) import admin/src/*
      # directly to validate its nav entries/routes, which pulls in admin's own
      # deps (e.g. lucide-react) — not installed by the root npm ci above.
      - name: Install admin dependencies
        run: cd admin && npm ci --legacy-peer-deps

      - name: Build (tsc)
        run: npm run build

      # The root TypeScript build does not compile the React admin SPA. Build it
      # explicitly so JSX/runtime-import mistakes on CRM pages cannot pass CI.
      - name: Build admin (Vite)
        run: npm run admin:build

      - name: Tenant-scoping lint (cross-tenant IDOR guard)
        run: npm run lint:tenant-scoping

      - name: Test + coverage thresholds (vitest)
        run: npm run test:coverage
