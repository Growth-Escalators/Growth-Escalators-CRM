# WizMatch Outbound OS — PR 2 review (Opus)

**Reviewed** 2026-07-26 · **Branch** `ge/outbound-02-policy-schema-service` ·
**Parent** `ge/outbound-01-prd-adrs` · **Reviewer** Claude (Opus), senior architect role
**Spec** PRD-005 §8, §9, §10, §22.2, §25 · ADR-006 · ADR-007 ·
readiness marker `.ai/OUTBOUND_PR2_SPEC_READY` (commit `b3aaee1`)

**Verdict: fix-then-ship.** The scope-inheritance engine and the schema are good work — per-dimension
inheritance is genuinely independent, the §10.10 composite-FK discipline is applied without a single
naked entity FK, and the §9 reason-code taxonomy matches the ratified table code-for-code in both
directions. But the migration **could not apply to any database**, and five of the gate's terminal
decision-assembly behaviours contradicted §8.6/§8.7/§8.9/§8.10. All are fixed in this branch; what
remains is verification that needs a live Postgres.

---

## 1. Branch and commit SHAs

**Implementation (Sonnet), reviewed as submitted**

| SHA | Subject |
|---|---|
| `9149def5a260376cb1a434fc1505698bac219cda` | feat(wizmatch): add outbound policy schema and migration |
| `0745ba799ec21299e157d51a97cff3285dc795c3` | feat(wizmatch): add policy resolver and gate contracts |
| `0465cf320b11e7255717ab20a13fd0dafdd0d489` | test(wizmatch): cover outbound policy schema and resolver |
| `1cf9ba42e59a874a8abb0d7cc510c527334a2ec1` | docs(wizmatch): record PR 2 implementation in the context layer |

**Corrective commits (this review)**

| SHA | Subject |
|---|---|
| `79bb384ff79527fe438ac9426e1d6156a824e783` | fix(wizmatch): order 0037 composite-FK targets before the FKs |
| `3057221b7820ac137696bb5f46d8f1699bfbd887` | fix(wizmatch): correct outbound gate decision assembly |
| `e690ac1a0b19a0cb5711e74acb9f42ead2a02ade` | test(wizmatch): pin the PR 2 migration and gate contracts |
| `810d1448d6cdc842f26857febebf355ca3651b9d` | fix(wizmatch): enforce L5 duplicate-suspect containment in the gate |

Method: three read-only Explore subagents in parallel (migration/Drizzle parity; tenancy/FKs/locks;
resolver/precedence/tests), with every Critical and High finding re-verified by hand against the
source before any fix was made. Two subagent findings were downgraded on that re-check (§9 below).

---

## 2. Implementation completeness against §22.2

| # | Criterion | Status |
|---|---|---|
| 1 | Seven new tables, `tenant_id` NOT NULL, indexes lead with `tenant_id` | ✅ (8 tables — §10.1–§10.7 is 7, §10.9.1 adds `wizmatch_suppression_events`; the criterion's own section list implies 8. PRD wording nit, not a defect) |
| 2 | All cross-table entity refs are composite FKs; no `scope_ref_id`; `signal_id` → `wizmatch_job_signals` | ✅ 29 composite entity FKs, **zero** naked single-column entity FKs |
| 3 | Six additive `(tenant_id, id)` unique indexes, non-partial, `IF NOT EXISTS` | ✅ |
| 4 | Every `users` reference `ON DELETE SET NULL`, column nullable | ✅ 10 of 10 |
| 5 | Every §10.1/§10.6.1/§10.6.2/§10.7/§10.9.1 CHECK, incl. D-7 evidence and `manually_released` | ✅ 33 CHECKs (one gap noted L-1) |
| 6 | No `admin_override`, no `suppression_scope` | ✅ |
| 7 | `wizmatch_suppression_list` UNIQUE (tenant_id, email) untouched | ✅ |
| 8 | 15-state CHECK; four partial predicates from one exported constant | ✅ code; ⚠️ was unpinned by any test → now pinned |
| 9 | Generated by `db:generate`; raw SQL confined to one marked guard block | ✅ (plus a second, explicitly marked *reorder* block — see C-1) |
| 10 | All ten §10.11.4 verifications with real output, incl. fresh `0000→0037` replay | ❌ **not run** — this is what hid C-1 |
| 11 | Journal `when` exceeds `1784464092263` | ✅ `1785039545644` |
| 12 | Not applied to production | ✅ |
| 13 | Both gate functions in one module; `PolicyDecision` branded and gate-only | ⚠️→✅ brand was forgeable (C-2); fixed |
| 14 | Resolver implements L0–L8 incl. L0/L1c, per-dimension inheritance, §8.1.1, fails closed | ⚠️→✅ L5 duplicate containment was missing (H-5); fixed |
| 15 | `resolveCompanyStatus()` has no legacy fallback | ✅ |
| 16 | Every company insert path writes the cold-start root row in the same transaction | ❌ **not implemented** — see B-1 |
| 17 | `buildScopeKey()` is the only producer of `scope_key` | ✅ |
| 18 | `npm run build` 0, `npm test` green | ✅ 99 files, 867 tests |
| 19 | Zero behavioural change; no caller migrated, no flag flipped | ✅ verified by grep |
| 20 | Every §20.1 schema/resolver/evidence/scope/tenancy test passes | ⚠️ partial — gaps in §8 |

---

## 3. Migration assessment

**Is 0037 next?** Yes. Journal `idx: 37`, `when: 1785039545644` > 0036's `1784464092263`, no gap or
collision; `meta/0037_snapshot.json` `prevId` chains correctly to `meta/0036_snapshot.json`.

**Additive?** Yes. Destructive-statement scan returns **zero** — no `DROP TABLE/COLUMN/INDEX/CONSTRAINT`,
no `TRUNCATE`, no `DELETE`, no `ALTER COLUMN … TYPE`, no `RENAME`. The single `DROP TRIGGER IF EXISTS`
sits inside the guard block and is immediately followed by `CREATE TRIGGER` — an idempotent recreate.

**Drizzle parity?** Excellent. Column-for-column, index-for-index, CHECK-for-CHECK across all 8 tables,
33 CHECKs and 31 indexes, `schema.ts` and the SQL agree.

**Tables created (8):** `wizmatch_company_policies`, `wizmatch_company_policy_events`,
`wizmatch_company_duplicates`, `wizmatch_reply_mailboxes`, `wizmatch_outreach_batches`,
`wizmatch_outreach_enrolments`, `wizmatch_outreach_events`, `wizmatch_suppression_events`.

**Existing tables touched:** `wizmatch_companies` (§10.8 ADD COLUMN + FK), `wizmatch_suppression_list`
(§10.8 two ADD COLUMNs + FK), and the six §10.10.1 additive indexes on `users`, `contacts`,
`contact_channels`, `wizmatch_companies`, `wizmatch_job_signals`, `wizmatch_requirements`. Nothing else.
No Growth, SEO or D2C table is altered.

**The blocker (C-1, fixed).** Every composite FK references a parent on `("tenant_id","id")`, and
drizzle-kit emits every `CREATE INDEX` **after** every `ADD CONSTRAINT`. PostgreSQL resolves the
referenced unique index at `ADD CONSTRAINT` time, not at `COMMIT`
(`tablecmds.c` `transformFkeyCheckAttrs`), so the first such statement raises SQLSTATE **42830**
*"there is no unique constraint matching given keys for referenced table"* and aborts the transaction.

- All **29** composite FKs were affected. Measured: 29 ordering violations before the fix, 0 after.
- The two FKs inside `DO $$ … EXCEPTION WHEN duplicate_object` blocks were **not** protected — that
  handler catches 42710, not 42830.
- No prior migration creates any `(tenant_id, id)` unique index, so none of the nine targets pre-existed.
  This was checked, not assumed; it is what rules out "it would have worked incrementally in prod".

Fix: the nine `CREATE UNIQUE INDEX` statements are **moved** ahead of the FK block inside a marked
`MANUAL REORDER (0037)` block. No statement text is altered, added or removed — `db:generate` still
reproduces the same set, only in the order Postgres cannot execute.

**Lock exposure (unchanged, G1-gated).** The three `CREATE UNIQUE INDEX` statements on `users`,
`contacts` and `contact_channels` are not `CONCURRENTLY` and take a write-blocking lock on Growth's
shared tables for the build duration. This is U-7, already an owner decision gating G1, and §10.11.4
requires the duration to be measured on a production-sized restore first.

---

## 4. Tenant-isolation assessment

Clean, and the strongest part of the schema work.

- All 8 new tables carry `tenant_id uuid NOT NULL REFERENCES tenants(id)`.
- **Every** index on the new tables leads with `tenant_id` — zero exceptions.
- **29 composite entity FKs, zero naked single-column FKs to an entity.** The 8 FKs to `tenants(id)`
  itself are counted separately and are correctly single-column.
- Every composite FK's target has a supporting `(tenant_id, id)` unique index — and, after C-1, one
  that exists by the time the FK is added.
- `scope_ref_id` is genuinely absent; `signal_id` references `wizmatch_job_signals`, not Growth's
  `signals`.
- Every query in `policyResolver.ts` and `outreachGate.ts` filters on `tenantId`.

§10.10's prose says "22 entity references" while its own table unpacks to 29 columns. The migration
matches the table, not the prose — a PRD counting nit (L-2), not a code defect.

---

## 5. Resolver assessment

**Phase 0 (inheritance) was correct as submitted.** `resolveDimension()` walks per dimension and takes
the first non-null value, recording `scopeKey` and `policyId` — not per row, which is the classic bug
here. The §8.1 worked example (a `location:bengaluru` row setting only `outreach_eligibility='paused'`
leaves the company-wide `relationship_type` and `external_hiring_policy` in force) holds and is tested.

- L0 missing root → `deny` / `policy_missing_root` / `effectiveLevel 0` / `effective` all null. ✅
- **No legacy fallback.** Zero references to `wizmatch_company_intelligence` or any legacy `.status`
  anywhere in `src/modules/outreach`. D-1/A-22 satisfied. ✅
- §8.1.1 unresolvable scope → `deny` / `scope_unresolvable`, checked on scope **type** existence rather
  than label match, which is the fail-closed reading. `business_unit` correctly denies any request
  without an explicit label. ✅
- L1c (non-overridable block at a narrower scope) scans the raw applicable rows rather than the
  Phase-0-resolved dimension, so a narrower `eligible` row cannot mask a hard block. ✅
- L1 keys on `is_non_overridable`, not `block_class`, per §8.3. ✅
- `campaign_family` is never consulted for permission. ✅
- `buildScopeKey()` is the sole producer of `scope_key`; normalisation is deterministic and total;
  malformed input throws rather than coercing; `region:` is constrained to `india|us` in both TS and a
  DB CHECK. ✅
- Root completeness and the no-op-row rule are enforced by DB CHECKs
  (`..._root_defines_all_chk`, `..._scoped_overrides_one_chk`). ✅

**Phase 1 (decision assembly) is where the defects were** — five of them, all fixed. See §9.

**Immutability.** The trigger protects every policy decision column and permits only `superseded_at`
and `superseded_by_policy_id` to change. Supersession therefore remains possible while a changed
decision must be a new row plus an event, exactly per D-3/A-24. ✅

---

## 6. Lock and duplicate assessment

**Enrolment states.** The 15 states in the SQL CHECK and in `WIZMATCH_ALL_ENROLMENT_STATES` are
identical, order for order. The 8 live states are exactly D-6's list, and all four partial-index
predicates carry the same 8. **A reply does not release the lock**: `replied`, `awaiting_action`,
`positive_reply`, `referral_received` and `conversation_open` are all in the live set, in both SQL and
TypeScript. The 7 terminal states release it. `manually_released` requires actor and reason, CHECK-
enforced on both the enrolment and the event row. ✅

The §10.6.2 constraints are all present: the company cold-email lock, the contact live-uniqueness, the
A-30 email-normalised predicate, and the A-20 non-cold overlap constraint.

**Duplicates.** The schema is sound — both company ids as composite FKs, `CHECK (company_a_id <
company_b_id)` preventing self-pairing and bidirectional double-recording, and a pair unique index.
But `wizmatch_company_duplicates` was **never read** (H-5): §8.2 L5 and §8.8 were unenforced, so a
company under duplicate review could still be queued, exported and sent. Now enforced, checking **both
sides** of the pair — because pairs are stored ordered, a `company_a_id`-only lookup would miss half of
all suspects. Free preparation still runs.

Remaining (PR 4, correctly out of scope): the duplicate review UI, merge / confirm-separate, and the
policy-divergence warning.

---

## 7. Suppression assessment

- `UNIQUE (tenant_id, email)` on `wizmatch_suppression_list` is **untouched** — no DROP, no replace. ✅
- `suppression_scope` does not exist; the table stays exact email/channel grain. ✅
- Grains are correctly separated per §8.5: a **hard bounce does not set `contacts.do_not_contact`**;
  unsubscribe is contact-grain; company-wide removal is a non-overridable `compliance` policy block.
  Three different grains, three different writes. ✅
- The union reads both grains (A-1). ✅ — but only the query input was lowercased, not the stored
  column (H-3, fixed).
- The inline suppression query at `wizmatchOutreachService.ts:183-189` is **still present and
  unchanged**, correctly — deleting it is PR 3 row 29, not PR 2. ✅

---

## 8. Test assessment

**Before:** 4 files, 37 tests, all passing. **After:** 6 files, 60 tests, all passing.
Full suite: **99 files, 867 tests green**; `npm run build` exits 0; `git diff --check` clean.

The submitted tests were real where they existed — the scoped-inheritance test asserts per-dimension
`scopeKey` provenance and would fail if the resolver were wrong, and `wizmatchReasonCodes.test.ts`
mechanically derives the six-code prep set from the taxonomy rather than restating it.

Three weaknesses found:

1. **The gate test's db mock discards `.where()` entirely**, returning the full fixture regardless of
   predicate. Every tenant-isolation and suppression-normalisation assertion in that file therefore
   *cannot fail* — including the one named as the A-1 regression. This is why H-3 survived a passing
   suite. The new contract suite uses a predicate-capturing mock.
2. **No test pinned the migration's SQL text to the exported state constants**, which §22.2 #8 and
   §10.6.2 both call for. The migration is necessarily a frozen copy, so only a test can hold them
   together.
3. The 8×8 routing test loops all 64 combinations but asserts only *shape*, so ~59 cells cannot fail on
   a wrong route or mode.

**New tests, each verified to fail on the defect it covers:**

- `wizmatchOutboundMigrationContract.test.ts` (8) — FK/index ordering (control: 29 violations detected
  on the pre-fix file, 0 after), state-list parity, destructive scan, untouched suppression index,
  absence of `admin_override`/`suppression_scope`/`scope_ref_id`.
- `wizmatchOutreachGateContract.test.ts` (15) — the §8.6/§8.7 routing outcomes, L1c derived prep,
  suppression column lowercasing, grain-accurate reason codes, L5 duplicate containment, and a
  tsc-checked `@ts-expect-error` for the brand. **Control run on the brand test:** with the string brand
  restored, `tsc` reports *"Unused '@ts-expect-error' directive"*; with the symbol brand it passes — so
  the test discriminates between the two designs rather than passing either way.

---

## 9. Findings

### Critical

**C-1 — Migration 0037 could not apply to any database.** *(fixed)*
All 29 composite FKs added before their `(tenant_id, id)` target indexes exist → SQLSTATE 42830 on the
first one, whole migration aborts. Neither `IF NOT EXISTS` nor the `duplicate_object` handlers help.
Root cause of it going unnoticed: §22.2 #10's fresh `0000→0037` replay was never run.
`src/db/migrations/0037_unknown_siren.sql:195` (pre-fix) · §10.11.4, §22.2 #9/#10

**C-2 — `PolicyDecision` was forgeable, so §8.10 rule 3 did not hold.** *(fixed)*
`readonly __brand: 'WizmatchPolicyDecision'` is a plain string field; TypeScript is structural, so any
caller could write that literal and fabricate an `allow`. The whole chokepoint design rests on a sender
being unable to do this. Now a module-private `unique symbol`.
`policyTypes.ts:108` (pre-fix) · §8.10 rule 3, §22.2 #13

### High

**H-1 — §8.6's "restricted is not uniformly denied" was defeated.** *(fixed)*
The terminal return zeroed `allowedCampaignTypes`/`allowedOutreachModes` whenever the compatibility
decision was `deny`, making §8.6's and §8.7's own tables unreachable: `msp_vms_only` lost its `msp_vms`
research route, `direct_hiring_only` lost `research_only`, and every account-managed relationship
override lost `account_managed`. §8.9 defines `[]` as "none permitted", not "denied".
`outreachGate.ts:286-287` (pre-fix) · §8.6, §8.7, §8.9

**H-2 — L1c hardcoded `preparationAllowed: true`.** *(fixed)*
A `compliance` block at a narrower scope kept enriching and preparing a company that had formally asked
to be removed — the continued-PII-processing failure §8.9/H-1 was written to prevent, reintroduced one
level down. Every other level derived it correctly.
`outreachGate.ts:206` (pre-fix) · §8.9, §22.2 #20

**H-3 — Suppression read lowercased the input but not the stored column.** *(fixed)*
§8.10 is explicit: "both sides lowercased at read". A row written by any path that bypasses
`normalizeChannelValue` carries mixed case and would never match — the exact A-1-class gap the gate
exists to close, and invisible to the test suite because the mock ignores predicates.
`outreachGate.ts:79` (pre-fix) · §8.10, §20.1

**H-4 — Reason codes did not name the real cause.** *(fixed)*
L7 reported `email_hard_bounce` for **every** suppression hit including `contacts.do_not_contact` —
§8.5 is explicit that a bounce is a channel-quality fact and an unsubscribe is a stated preference, so
this writes a false fact into the audit trail PR 3 will depend on. Separately, every review reported
`policy_unknown_cold_start` regardless of cause, and terminal denies carried **no reason code at all**.
`outreachGate.ts:278, 288` (pre-fix) · §8.5, §9

**H-5 — L5 duplicate-suspect containment was not implemented.** *(fixed)*
`wizmatch_company_duplicates` was created by 0037 and never read, so A-14 and §8.8 were unenforced.
§22.2 #14 requires L0–L8. Now enforced on both sides of the ordered pair.
`outreachGate.ts:18-20` (pre-fix) · §8.2 L5, §8.8, §22.2 #14

### Medium

**M-1 — `campaignCompatibility.ts` kept a second hand-written `preparationAllowed` list.** *(fixed)*
Exactly the "second hand-written list" §8.9/H-1 forbids. Values happened to agree with the taxonomy,
but nothing tied them together and no test cross-checked them. Now derived via `isPreparationAllowed`.

**M-2 — `accountOwnerUserId` was always `null`.** *(fixed)*
§8.7 routes `existing_client` to `wizmatch_companies.account_owner_user_id`, so the `account_owner`
route was returned with no owner — unactionable. Now populated, with the documented null fallback.

**M-3 — `former_client` never contributed `reengagement`.** *(fixed)*
§8.7 names it; `RelationshipOverride` had no slot for a campaign-type contribution.

**M-4 — No test pinned the SQL state lists to the exported constant.** *(fixed)* §22.2 #8, §10.6.2.

**M-5 — The gate test mock discards `.where()`.** *(mitigated, not removed)*
The new contract suite captures predicates; the original suite still does not. Worth converging in PR 3.

**M-6 — `lower(email)` defeats the `(tenant_id, email)` index.** *(open, PR 3)*
The H-3 fix makes the suppression read a sequential scan. Correct beats fast here — but PR 3 should add
an expression index on `lower(email)` before any caller runs this per-recipient in a send loop. Not
added in PR 2: it would mean a new statement in a spec-frozen migration.

**M-7 — The resolver defaults missing dimensions via `??` rather than failing closed.** *(open)*
`relationshipType ?? 'new_prospect'` and `hiringPolicy ?? 'unknown'` rely entirely on the DB CHECKs. The
CHECKs are present and correct, so this is defence-in-depth, not a live hole — but §8.10 rule 5 names
"malformed persisted enum" as a fail-closed case and this would silently allow instead.

### Low

**L-1 — `evidence_kind` has no enumerated CHECK.** The D-7 CHECK asserts `evidence_kind IS NOT NULL` but
never `IN (…the six…)`. This matches the PRD's own SQL sketch, so it is a spec gap rather than a
deviation — but every other categorical column in this migration has an enum CHECK.

**L-2 — §10.10 says "22 entity references"; its own table unpacks to 29.** PRD wording.

**L-3 — §22.2 #1 says "seven new tables" but cites §10.1–§10.7 *and* §10.9.1, which is eight.** PRD wording.

**L-4 — No index supports a `company_b_id`-only duplicate lookup.** The new L5 query uses
`OR (company_a_id, company_b_id)`; at current row counts this is fine, but it is a scan.

---

## 10. Fixes made

| Commit | What |
|---|---|
| `79bb384` | Moved the nine `(tenant_id, id)` unique indexes ahead of the FK block. Statement **order** only — no statement rewritten, added or removed. |
| `3057221` | Symbol brand (C-2); allowed-lists no longer zeroed on deny (H-1); L1c prep derived (H-2); `lower()` on the stored column (H-3); grain-accurate and cause-accurate reason codes (H-4); taxonomy-derived prep in the routing table (M-1); `accountOwnerUserId` populated (M-2); `former_client` → `reengagement` (M-3). |
| `e690ac1` | Two contract suites, 23 tests, each verified to fail on the defect it covers. |
| `810d144` | L5 duplicate containment, both sides of the pair (H-5), + 4 tests. |

No caller was wired. No flag was changed. No route was touched. No migration was applied.

---

## 11. Remaining blockers

**B-1 — §22.2 #16 is not implemented.** *"Every company insert path writes the cold-start root policy
row in the same transaction."* No such write exists in this branch. Without it, §21.2 condition 4
("zero companies missing an effective policy") is satisfiable only for the instant after the backfill,
and every company discovered afterwards immediately re-breaks it — which is precisely the failure D-1
and A-30 were written to prevent. **Not fixed here** because it means editing company-insert call sites
outside `src/modules/outreach`, which is caller-adjacent work and needs an owner call on whether it
lands in PR 2 or PR 3. It must land **before** G2/G4 either way. *This is the one criterion still open.*

**B-2 — §22.2 #10's ten verifications have not been run.** Nine of ten need a live Postgres. The two
that were checkable from the file are now automated (ordering, state parity). The fresh `0000→0037`
replay, the production `information_schema` drift diff, and the shared-table lock measurement remain.

**B-3 — The immutability trigger is untested.** Its body was read and reasoned through and is correct —
it protects every decision column and permits only supersession metadata — but no test fires it. Needs
a live database.

**B-4 — §20.1 categories with no test:** tenant isolation of the resolver (the mock cannot express it),
a genuine thrown DB error on the fail-closed path, `business_unit` applicability (only `location` is
covered), and per-cell values for ~59 of the 64 routing combinations.

---

## 12. Required steps before applying 0037 (G1)

1. **U-7 owner sign-off** on the three additive `(tenant_id, id)` indexes on `users`, `contacts` and
   `contact_channels` — shared with the Growth tenant. Still open; explicitly blocks G1, not PR 2.
2. **Run the fresh `0000→0037` replay on a scratch database** and paste real output. This is the check
   that would have caught C-1; do not accept the fix on reasoning alone, including mine.
3. **Measure the index-build lock** on a production-sized restore of `users`, `contacts` and
   `contact_channels`, and decide `CONCURRENTLY` versus a maintenance window from that number.
4. **Run the production `information_schema` drift diff** and confirm none of the nine target indexes
   already exists under a different name.
5. Re-run the destructive-statement scan and confirm zero (automated: the contract suite).
6. Fire the immutability trigger against the scratch database — one decision-column `UPDATE` must be
   rejected, one supersession-metadata `UPDATE` must succeed.
7. Confirm `WIZMATCH_SENDING_ENABLED` and `AUTOMATED_EMAILS_ENABLED` are still off.

---

## 13. Required steps before PR 3

1. **Close B-1** — the cold-start root-policy write on every company insert path. PR 3 is the natural
   home now that PR 2 has shipped the schema; confirm with the owner.
2. **Add the `lower(email)` expression index (M-6)** before any caller runs the suppression read
   per-recipient.
3. **Converge the two gate test mocks (M-5)** so the original suite can also detect a wrong predicate.
4. **Close M-7** — explicit `policy_resolver_error` on an unexpectedly-null root dimension.
5. Then, and only then, §22.3: all 30 rows of the §8.10.1 caller-migration checklist, the deletion of
   the inline suppression query at `wizmatchOutreachService.ts:183-189`, the A-1/A-4/mailer fixes, the
   unsubscribe HMAC normalisation, and the shadow-vs-enforce equivalence harness.

---

## 14. Final recommendation

**Approve PR 2 with the four corrective commits, on the explicit condition that B-1 and B-2 are closed
before G1/G2.** The design is sound and the schema is the strongest tenancy work in this repo. The
defects were concentrated in two places — statement ordering the generator could not get right, and the
terminal decision assembly — and both are now covered by tests that fail on the defect rather than tests
that merely pass.

The process lesson is worth recording: **every one of C-1, H-3 and H-5 survived a fully green test
suite.** C-1 because the required replay was skipped; H-3 because the mock discards the predicate it was
supposedly asserting on; H-5 because nothing tested a table that nothing read. A green suite that cannot
fail is not evidence. §22.2 #10 exists for exactly this reason and should not be waived again.

Do not apply 0037. Do not promote enforcement. Do not begin PR 3 until B-1 has an owner decision.

---

## 15. PR 2 closeout — 2026-07-26 (Sonnet, following Opus review)

Closes B-1, B-2 and B-3, and M-6, using three read-only Explore subagents (company-insert
paths; suppression-index placement; local Postgres setup) followed by main-session edits and a
real local verification pass. No caller migrated, no flag changed, no route touched, nothing
applied to Railway or any shared database.

**B-1 closed — root policy bootstrap (§22.2 #16).** New module
`src/modules/outreach/companyBootstrap.ts` exports `WIZMATCH_ROOT_POLICY_DEFAULTS` (the exact
row: `entire_company`/`entire_company`, `needs_review`, `unknown`, `new_prospect`, `standard`,
`is_non_overridable=false`, `source='deterministic_rule'`), `insertWizmatchCompanyRootPolicy`
(idempotent `ON CONFLICT (tenant_id, company_id, scope_key) WHERE superseded_at IS NULL DO
NOTHING`, so concurrent bootstrap attempts cannot create two active root rows), and
`withWizmatchCompanyTransaction` (BEGIN/COMMIT on a dedicated `pool.connect()` client).

Both confirmed production company-insert paths now call it:
- `wizmatchSourcing.ts` `ingestWizmatchSignals` — the upsert's `RETURNING id, (xmax = 0) AS
  inserted` distinguishes a genuine insert from the `ON CONFLICT DO UPDATE` branch, so the root
  policy is written once, only on creation, never on every signal that touches an existing
  company. Reachable from the signal-ingest route, the ATS poller, and the TheirStack importer.
- `wizmatchContactIntelligenceRepo.ts` `seedProspectCompany` — the "not existing" branch now
  runs inside `withWizmatchCompanyTransaction`, so the company row and its root policy commit or
  roll back together (this file already had a `pool.connect()` precedent via
  `withContactDiscoveryAdvisoryLock`, so a real transaction was used here rather than two
  independent statements).

No allow policy is ever created; no backfill was run; existing companies are untouched (verified
by the "never writes a root policy on the existing-company (update) branch" /
"does not write a root policy when the company upsert only updates an existing row" tests in both
files). A new gate-contract test pins the literal `WIZMATCH_ROOT_POLICY_DEFAULTS` row through
`evaluateWizmatchOutreachGate` and asserts the decision is `review` (never `allow`), with
`reasonCodes: ['policy_unknown_cold_start']` — the exact cold-start behaviour PRD-005 §22.2 #16
requires.

**M-6 closed — suppression expression index.** `0037_unknown_siren.sql` now adds
`CREATE INDEX IF NOT EXISTS "wizmatch_suppression_tenant_lower_email_idx" ON
"wizmatch_suppression_list" USING btree ("tenant_id", lower("email"))`, placed after the
suppression FK guard block (table/columns already exist by that point) and before the immutability
trigger's guard block. The pre-existing `UNIQUE (tenant_id, email)` index is untouched — confirmed
by both the existing "leaves ... untouched" test and a new test asserting the new index name is
distinct and never `CREATE UNIQUE`. `schema.ts` is deliberately NOT changed (no `lower()`
expression-index precedent exists anywhere in the file, and drizzle-kit cannot reliably round-trip
one) — this is hand-authored migration-only SQL, matching the existing precedent for the
immutability trigger. `npm run db:generate` still reports "No schema changes, nothing to migrate."

**B-2 closed — real local Postgres verification, `127.0.0.1:5432`, disposable databases only.**
Target confirmed local before every command (never Railway, never a public hostname). Two
disposable databases (`wizmatch_pr2_verify_full`, `wizmatch_pr2_verify_incremental`) plus three
short-lived timing databases, all dropped after recording results.

- **Fresh `0000→0037` replay:** `npx drizzle-kit migrate` against a brand-new database —
  succeeded (`migrations applied successfully!`). `drizzle.__drizzle_migrations` shows 38 rows,
  `created_at` monotonically increasing, matching `meta/_journal.json`'s `when` values exactly
  (`...464092263` for 0036, `...039545644` for 0037).
- **Incremental apply:** 0037 was moved out of `src/db/migrations/` and its journal entry removed,
  0000→0036 applied to a second disposable database, then 0037 (file + journal entry) restored and
  applied alone — succeeded independently.
- **Re-apply is a no-op** on both databases (drizzle-kit's applied-hash tracking skips already-run
  migrations) — the idempotent-migration requirement holds.
- **Composite FKs:** `\d wizmatch_company_policies` on the fresh database shows all 5 composite
  `(tenant_id, x_id)` FKs plus the `tenants(id)` FK, matching `schema.ts`.
- **Destructive-statement scan:** already automated in
  `wizmatchOutboundMigrationContract.test.ts` (zero matches); re-confirmed by direct read of the
  file during this session.
- **Timing (disposable, empty dataset — NOT a production-lock measurement):** fresh full replay
  ≈0.8s wall including Node/drizzle-kit startup; isolated 0037-only apply ≈0.47s wall. These
  numbers are close to tool-startup noise floor and say nothing about the `users`/`contacts`/
  `contact_channels` index-build lock duration on production-sized tables — that measurement
  (§10.11.4 requirement, gating U-7/G1) still requires a production-sized restore, which this
  session correctly did not attempt.

**B-3 closed — immutability trigger fired against a live database.** On the fresh database: an
`UPDATE ... SET outreach_eligibility='blocked'` against an existing root policy row raised
`wizmatch_company_policies is immutable except superseded_at/superseded_by_policy_id` (trigger
rejection, as designed); a second row's `UPDATE ... SET superseded_at = NOW(), superseded_by_policy_id
= <id>` (supersession-metadata-only) succeeded.

**Root-policy CHECK constraints fired against a live database:** a complete `entire_company` row
(all three dimensions) succeeded; an incomplete one (`external_hiring_policy`/`relationship_type`
omitted) raised `wizmatch_company_policies_root_defines_all_chk`; a scoped `location` row
overriding one dimension (with a `review_date`, required by the `paused` state) succeeded; a scoped
row with no dimension set raised `wizmatch_company_policies_scoped_overrides_one_chk`.

**Other constraints fired against a live database:** `wizmatch_company_duplicates`'s ordered-pair
CHECK rejected a self-pair (`company_a_id = company_b_id`); the cold-email lock partial unique
index (`wizmatch_outreach_enrolments_company_cold_email_lock_uniq`) rejected a second live
`cold_email` enrolment for the same company while the first stayed `queued`; the batch/contact
uniqueness index similarly rejected a duplicate live enrolment for the same contact.

**Remaining, unchanged from the original review:** U-7 (owner sign-off on the three shared-table
indexes) and the production-sized lock-duration measurement still gate G1, not PR 2 — this session
did not touch Railway, did not apply 0037 anywhere but a disposable local database, and did not run
the existing-company backfill. B-4's remaining test gaps (full tenant-isolation-of-the-resolver
coverage, ~59 of 64 routing-matrix cells, `business_unit` applicability) are unchanged and remain
open for a later PR.

**Tests:** `npm run build` exit 0; `npm test` → 99 files / 873 tests green (873 = prior 867 + 6 new:
2 in `wizmatchSourcing.test.ts`, 2 in `wizmatchContactIntelligenceRepo.test.ts`, 1 in
`wizmatchOutreachGateContract.test.ts`, 2 in `wizmatchOutboundMigrationContract.test.ts` minus one
consolidated — see commit diff for the exact count); `git diff --check` clean; `npm run db:generate`
reports no schema drift.

**Updated verdict: PR 2 is now fully closed against §22.2.** The one previously-open criterion
(#16) is implemented and tested; the ten §10.11.4 verifications have real output recorded above,
with the explicit exception of the production-sized lock measurement, which was never in scope for
a local disposable database and remains a G1 prerequisite. Nothing was pushed, merged, deployed, or
applied outside a disposable local database.

---

## 16. Final independent PR 2 readiness review — 2026-07-26 (Opus)

**Reviewed** `a94d19b` (closeout HEAD) → corrective commit `102b657` · **Branch**
`ge/outbound-02-policy-schema-service` · Three read-only Explore subagents (migration/DB;
bootstrap/tenancy; resolver/suppression/tests) plus main-session verification against a
**disposable local PostgreSQL 16** at `127.0.0.1` (created and dropped in-session).

**Verdict: ship, after the three §22.2 #16 gaps found here and fixed in `102b657`.**
The §15 closeout claimed criterion #16 closed. It was closed on two of **three** company-insert
paths, and on neither of them in a way that met the criterion's literal "same transaction" wording
for the highest-volume path. That is now genuinely closed and verified against a real database
rather than by reasoning.

### 16.1 Final PR 2 completeness against §22.2

All twenty criteria satisfied, with one documented spec contradiction in #10 (see §16.8).

| # | Criterion | Verdict |
|---|---|---|
| 1–8 | Schema: tables, 22/29 composite FKs, six additive indexes, users SET NULL, CHECKs, no `admin_override`/`suppression_scope`, suppression unique index untouched, 15-state CHECK from one constant | **PASS** |
| 9–12 | Migration: generated + single marked guard block, §10.11.4 verifications, journal `when`, not applied to production | **PASS**, except #10's production drift diff (§16.8) |
| 13–17 | Service: one module, branded `PolicyDecision`, L0–L8, no legacy fallback, **every** insert path bootstraps, `buildScopeKey` sole producer | **PASS** (13–15, 17 were already met; **16 fixed here**; **17 fixed here**) |
| 18–20 | `npm run build` 0, `npm test` green, zero behavioural change, §20.1 tests pass | **PASS** |

### 16.2 Migration verdict — **sound**

Independently re-verified, not taken on the prior report's word. All nine `(tenant_id, id)` unique
indexes are created at SQL lines 205–219, every composite FK at 221+, so C-1 is genuinely fixed;
29 composite entity FKs, zero naked entity FKs; journal `when` 1785039545644 > 1784464092263;
additive-only (the sole `DROP` is `DROP TRIGGER IF EXISTS` inside the guard block, the standard
replace-trigger idiom); `schema.ts` agrees with the SQL except the two pre-authorised
migration-only artefacts (the immutability trigger, and the `lower(email)` expression index).
`npm run db:generate` reports "No schema changes, nothing to migrate."

**Re-run for real this session** on a fresh disposable database: `npx drizzle-kit migrate`
applied **38 migrations** (0000→0037) successfully. Confirmed on that database:
`wizmatch_suppression_tenant_email_uniq_idx` still `CREATE UNIQUE INDEX ... (tenant_id, email)`;
the new `wizmatch_suppression_tenant_lower_email_idx` is non-unique on `(tenant_id, lower(email))`;
`wizmatch_company_policies_active_scope_uniq` is the partial unique index
`(tenant_id, company_id, scope_key) WHERE superseded_at IS NULL`.

### 16.3 Tenancy verdict — **clean**

No cross-tenant lookup found: every new-table read and the bootstrap helper key on `tenant_id`,
and every entity reference is a composite FK onto a `(tenant_id, id)` unique index, so a child row
cannot point at another tenant's parent. No Growth or legacy company path changed behaviour — the
only non-test edits outside `src/modules/outreach` are the three company-insert paths, and all
three changes are additive bootstrap writes.

### 16.4 Root-bootstrap verdict — **three gaps found, all fixed in `102b657`**

Insert paths were enumerated independently rather than trusting the PR description. **Three** exist:

| Path | Before this review | After |
|---|---|---|
| `wizmatchSourcing.ts` `ingestWizmatchSignals` (route + ATS poller + TheirStack importer) | bootstrapped, but as a **second statement on the shared pool** — not one transaction | one data-modifying-CTE statement, atomic |
| `wizmatchContactIntelligenceRepo.ts` `seedProspectCompany` | correct — real `BEGIN`/`COMMIT` | unchanged |
| `scripts/onboarding/wizmatch-seed-ats-boards.ts` | **no root policy at all — missed entirely** | bootstraps inside the `BEGIN`/`COMMIT` it already held |

Both gaps had the same consequence and it is worse than it first looks: the affected company is
permanently L0 `policy_missing_root` denied (fail-closed, so **no wrong send** — this was never a
safety hole) and **unrepairable by re-running**. On the sourcing path the next upsert takes the
`ON CONFLICT DO UPDATE` branch, so `(xmax = 0)` is false and the bootstrap never fires again; in
the seed script the `WHERE NOT EXISTS` guard never re-inserts the row. Only the PR 4 backfill could
have recovered them — which is precisely the §21.2 condition 4 failure D-1/A-30 exist to prevent.

Two further spec deviations fixed in the same commit: the persisted row omitted
`reason_code='policy_unknown_cold_start'`, which PRD §8.1 and ADR-006 both specify (permitted with
no evidence — the row is neither permanent nor non-overridable, so `..._evidence_required_chk`
holds); and `scope_key` was a duplicated literal rather than `buildScopeKey()` output (#17).

Verified on the disposable database **through the real production code path**, not a mock: a new
company wrote exactly `entire_company` / `entire_company` / `needs_review` / `unknown` /
`new_prospect` / `standard` / `is_non_overridable=false` / `deterministic_rule` /
`policy_unknown_cold_start`; re-ingesting the same company left the policy count at 1 and the
company count at 1; "companies missing an active root policy" = 0; and a second active root row was
rejected by `wizmatch_company_policies_active_scope_uniq`, so concurrency safety is **database**-
enforced, not a check-then-insert race.

### 16.5 Resolver verdict — **sound**

L0–L8 including L0 and L1c, §8.1.1 applicability, and genuinely independent per-dimension
inheritance (a location-scoped `paused` row supplies `outreachEligibility` while `relationshipType`
still resolves from the root). Missing root → `deny` + `policy_missing_root` with no legacy
fallback. `wizmatch_company_intelligence.status` is never read by the gate, so it cannot grant
permission. Non-overridable root blocks short-circuit before any narrower row. Campaign types and
modes survive a `deny` as explanatory compatibility data (§8.9's `[]` means "none permitted", not
"denied"). Exactly one `try/catch`, wrapping the whole evaluation, with every DB call inside it and
`deny` + `policy_resolver_error` as the only outcome — no error path can produce an `allow`.
Duplicate suspects are read **and** enforced on both sides of the ordered pair. `PolicyDecision` is
branded with a module-private `unique symbol`, so it is unforgeable outside the gate. No PR 3 caller
wiring exists: nothing outside `src/modules/outreach` and `src/__tests__` imports the gate or
resolver.

### 16.6 Suppression verdict — **grain-correct, index genuinely used**

Hard bounce suppresses only the exact lowercased email/channel, tenant-scoped — no domain or
company widening in the gate. Personal unsubscribe resolves at contact grain
(`contacts.do_not_contact` → `email_unsubscribed`, a stated preference, never reported as a bounce).
Company removal is a non-overridable `compliance` block caught at L1/L1c. Nothing broadened: the
only change to `wizmatchContactIntelligenceRepo.ts` across all of PR 2 is the transaction wrapper —
no suppression query was touched.

The `lower(email)` index is not merely present but **used**: on the disposable database,
`EXPLAIN` for the gate's exact predicate returns
`Index Scan using wizmatch_suppression_tenant_lower_email_idx`
with `Index Cond: ((tenant_id = ...) AND (lower(email) = ...))`. This retires M-6 with evidence
rather than with a textual match.

### 16.7 Test verdict — **the contract suites are real; two residual blind spots**

Gates run this session, on this branch, after `npm run admin:install` was already satisfied:
`git diff --check` clean · `npm run build` **exit 0** · `npm test` **100 files / 876 tests passed**
· `npm run db:generate` **"No schema changes, nothing to migrate."**

Checked specifically:
- **Migration-ordering test** — parses the real SQL, extracts every composite FK's statement index
  and its parent index's statement index, and asserts index-before-FK, with a
  `compositeFkRefs.length >= 25` anti-vacuity guard. Reverting `79bb384` makes it red.
- **`PolicyDecision` branding** — fails against a forgeable string brand, but via **`tsc`**, not
  vitest: it is an `@ts-expect-error` over a structurally-complete object, and a string brand would
  make the directive unused and fail the build. Real, but only while `npm run build` stays a gate
  (LOW note below).
- **Suppression-index contract** — asserts non-unique explicitly, not merely that an index exists.
- **Cold-start defaults** — imports `WIZMATCH_ROOT_POLICY_DEFAULTS` rather than re-typing literals,
  so the two cannot drift.
- **Duplicate containment** — captures the real drizzle condition tree and asserts it contains
  `' or '`; deleting the two-sided lookup makes it red.
- **Bootstrap coverage** — new `wizmatchCompanyBootstrapCoverage.test.ts` asserts at **source
  level** that every file inserting a company also bootstraps, because a per-call-site behavioural
  test can only ever cover the sites someone remembered — which is exactly how the ATS path was
  missed. **Verified non-vacuous**: with the seed script's bootstrap removed, the suite goes red;
  restored, green.
- **Sourcing atomicity** — now asserts the *predicate* (`FROM upserted WHERE upserted.inserted`,
  `(xmax = 0) AS inserted`, `WHERE superseded_at IS NULL DO NOTHING`) and that only **one**
  statement is issued, rather than trusting a mock's `inserted` flag — the mock cannot tell us what
  Postgres would do.

### 16.8 Findings

**Critical** — none.

**High** — none outstanding. Two were found and fixed here:
- **H-6 — `scripts/onboarding/wizmatch-seed-ats-boards.ts` created companies with no root policy.**
  *(fixed, `102b657`)* Missed by the §15 closeout. Only creator of ATS-linked companies; permanent
  unrepairable L0 deny. §22.2 #16.
- **H-7 — `ingestWizmatchSignals` bootstrapped in a second statement, not one transaction.**
  *(fixed, `102b657`)* Highest-volume creation path; a failure between the two statements committed
  a company that re-ingestion could never repair. §8.1, §22.2 #16.

**Medium**
- **M-8 — the persisted root row omitted `reason_code`.** *(fixed, `102b657`)* §8.1/ADR-006 name
  `policy_unknown_cold_start` on the row. Harmless at resolve time (the resolver derives it) but a
  raw table read or the PR 4 backfill's reconciliation would not have matched the documented row.
- **M-9 — `wizmatch_company_policies.reason_code` has no CHECK against the taxonomy.** *(open,
  PR 3/4)* Not reachable today — the bootstrap is the only writer and it uses a frozen constant —
  but `isPreparationAllowed()` returns `true` for an unrecognised code, so once a policy-write API
  exists a typo'd code would silently **permit** preparation instead of failing closed. Close this
  with the write API, not before.
- **M-10 — §22.2 #10's production `information_schema` drift diff has not been run.** *(open, G1)*
  Its literal text places it in PR 2, but it needs production read access that PR 2 is explicitly
  forbidden to take, and §22.2 #12 puts all production interaction in G1. Both earlier review
  passes dispositioned it as a G1 step (§12 step 4). Treated the same way here, and called out so
  it is not mistaken for silently dropped. **Recommend the owner fold it into the U-7 sign-off and
  correct §22.2 #10's wording in a later docs-only pass.**
- **M-7 — resolver defaults missing dimensions via `??`.** *(open, unchanged)* Defence-in-depth
  only; the DB CHECKs make it unreachable.
- **M-5 — the original gate suite's mock discards `.where()`.** *(open, unchanged)* Mitigated by
  the contract suite, not removed.

**Low**
- **L-5 — the branding guarantee is enforced by `tsc`, not vitest.** Both are mandatory gates
  today; flagged only so nobody trims CI to `vitest` alone and silently loses it.
- **L-6 — predicate capture was not extended to pre-existing filters.**
  `isNull(wizmatchCompanyPolicies.supersededAt)` (`policyResolver.ts:65`),
  `eq(wizmatchOutreachEnrolments.outreachMode, 'cold_email')` (`outreachGate.ts:176`) and
  `readAccountOwnerUserId`'s tenant/company predicate are exercised for outcome but never
  predicate-checked; delete any of them and the suite stays green. A superseded block row leaking
  back in as active is the failure that would slip through. Cheap follow-up: read back the
  `capturedWhere` entries the contract mock already collects.
- **L-1, L-2, L-3, L-4** — unchanged from §9.

### 16.9 Fixes made during this review

| Commit | What |
|---|---|
| `102b657` | H-6 (ATS seed script bootstrap), H-7 (single-statement atomic bootstrap on the sourcing path), M-8 (`reason_code` on the persisted row), #17 (`buildScopeKey` as sole `scope_key` producer), shared column list/arbiter/defaults between the standalone and CTE forms, source-level bootstrap-coverage test, sourcing tests converted from mock-return assertions to predicate assertions. |

No schema or migration file was edited. No caller was wired onto the gate. No flag was changed.
No route was touched. Nothing was applied to production or any shared database.

### 16.10 Remaining G1 prerequisites

1. **U-7 owner sign-off** on the three additive `(tenant_id, id)` indexes on `users`, `contacts`,
   `contact_channels` (shared with Growth).
2. **Production-sized lock measurement** on those three tables, and the `CONCURRENTLY`-vs-window
   decision from that number. Explicitly **not** measured: this session's timings are on empty
   disposable databases and say nothing about production.
3. **Production `information_schema` drift diff** (M-10) — confirm none of the nine target indexes
   already exists under another name.
4. Confirm `WIZMATCH_SENDING_ENABLED` and `AUTOMATED_EMAILS_ENABLED` are still off.

### 16.11 Remaining PR 3 prerequisites

1. Close **M-9** (taxonomy CHECK or validator) as part of the policy-write API.
2. Close **M-7** — explicit `policy_resolver_error` on an unexpectedly-null root dimension.
3. Close **L-6/M-5** — extend predicate capture to the `supersededAt` and `outreachMode` filters and
   converge the two gate mocks.
4. Then §22.3: the 30 rows of the §8.10.1 caller-migration checklist, deletion of the inline
   suppression query at `wizmatchOutreachService.ts:183-189`, the A-1/A-4/mailer fixes, unsubscribe
   HMAC normalisation, and the shadow-vs-enforce equivalence harness.
5. B-4's remaining §20.1 gaps (resolver tenant isolation, `business_unit` applicability, ~59 of 64
   routing cells) — unchanged.

### 16.12 Final ship recommendation

**Mark PR 2 ready for stacked draft review at `102b657`.** Every §22.2 criterion is satisfied, with
M-10 the single documented exception whose literal text PR 2 is forbidden to satisfy and which the
PRD's own #12 gates behind G1. The schema remains the strongest tenancy work in this repo, and the
one criterion the closeout got wrong (#16) is now correct on all three paths and verified against a
real PostgreSQL rather than argued for.

The process lesson from §14 repeated itself and is worth recording twice: **the §15 closeout
declared #16 closed with a green suite, and it was wrong on two of three paths.** The miss was
structural — it tested the call sites it knew about. The fix is the source-level coverage test,
which fails on a path nobody wrote a test for. Behavioural tests prove the paths you enumerated;
only a contract over the whole tree proves the enumeration.

Do not apply 0037. Do not promote enforcement. Do not begin PR 3 before U-7 and the G1 items above.
